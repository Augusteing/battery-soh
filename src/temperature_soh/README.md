# temperature_soh：温度感知 SOH 估计（最终参赛模型）

## 1. 项目定位

在 `partial_soh`（Scientific Reports 2026 复现，3 通道基线）的基础上，
构建**温度感知**的片段级 SOH 估计模型，作为参赛最终方案。

与基线相比，本模块的增量**只有温度通道**：

1. **温度通道**：模型输入从 3 通道 `(I, V, Q)` 扩展为 4 通道 `(I, V, Q, T)`；
2. **训练集与 partial_soh 完全一致**：只用 Severson（MATR）123-cell
   训练，不加任何其他数据集；SNL / HUST 仅在测试期另行考虑。
3. **Arrhenius 物理约束（后续阶段）**：把“温度越高、退化越快”的
   电化学规律作为物理先验注入损失函数（暂缓，先验证温度通道本身）。

## 2. 数据协议（训练只用 Severson）

| 数据集 | 电池数 | 温度 | 角色 |
| --- | --- | --- | --- |
| MATR（Severson） | 123（Scientific Reports 口径） | 30°C 恒温 | **唯一训练集** |
| HUST | 77 | 30°C 恒温 | 测试期候选，当前不使用 |
| SNL | 18（LFP） | 15/25/35°C | 测试期候选，当前不使用 |

训练/验证/测试划分沿用 partial_soh（按电池留出，Severson 123-cell）。
SNL / HUST 待测试期再做跨数据集验证，不进入训练。

## 3. SOH 标签口径（对齐原论文，只加温度通道）

统一采用 Scientific Reports 2026 原论文口径：

```
SOH(k) = Q_charge(k) / Q_nominal（1.1 Ah）
```

即“当前循环可充电容量 / 标称容量”，对应 MATR 的 Qc
（逐点累计充电容量取末值）。

明确不使用放电口径：`Qd / Qd(cycle 2)` 是 World Model 论文的口径，
本模块与其无关。

Severson 电池标称 1.1 Ah，实际充电容量约 1.06~1.07 Ah，
因此 MATR 的起始 SOH 约 0.96~0.97。

## 4. 模型架构（规划）

```
片段 (101, 4)  [I, V, Q, T]
   │
   ▼
共享编码器：Linear(4→128) → ReLU → Linear(128→32)
   │
   ▼
LSTM(input=32, hidden=64)          ← 极化状态/退化状态
   │
   ├── 电压预测头（预训练，稠密监督）
   └── SOH 头 [h; c]（微调，稀疏监督）

物理约束：Arrhenius 正则项
  退化速率 ∝ exp(−Ea / (R·T))
```

训练流程沿用迁移学习：电压预测预训练 → SOH 头微调，
同 `partial_soh` 的 Trainer。

## 4.1 温度通道对预训练/微调的影响（三行消融设计）

加入温度通道是否真的有用，用下面这张表回答（固定 seed、batch、
训练轮数，只开关温度通道与物理约束）：

| 配置 | 预训练 loss | 微调 SOH MAE（主协议） | 结论 |
| --- | --- | --- | --- |
| 3 通道（当前基线） | ？ | ？ | 对照 |
| 4 通道，无物理约束 | ？ | ？ | 温度通道本身有没有用 |
| 4 通道 + Arrhenius | ？ | ？ | 物理约束是否带来额外增益 |

判断标准：

- 若第 2 行预训练 loss 低于第 1 行，说明模型确实把温度用起来了
  （温度影响电压/极化，预训练能捕获）；
- 若第 2 行 SOH MAE 低于第 1 行，说明温度信号传递到了 SOH 回归；
- 若第 3 行进一步下降，说明物理约束在小样本变温条件下有正则价值。

工程注意：温度通道标准化不用全体 z-score（会被 200 块 30°C 恒温电池
主导），改用物理量纲归一化 `T' = (T − 25) / 10`。

## 5. 目录职责

| 目录 / 文件 | 职责 |
| --- | --- |
| `DataLoader/` | Severson 片段构建（读取、切分、标签、划分） |
| `Trainer/` | 4 通道模型、Arrhenius 物理约束、预训练/微调、评估 |

## 6. 路线图

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M1 | Severson .mat 统一读取 | 已完成 |
| M2 | 片段切分（4 通道）+ SOH 标签 + 数据划分 | 待做 |
| M3 | 4 通道模型 + Arrhenius 物理约束 | 待做 |
| M4 | 预训练/微调训练 | 待做 |
| M5 | 消融（温度通道 on/off、物理约束 on/off）+ 报告素材 | 待做 |
